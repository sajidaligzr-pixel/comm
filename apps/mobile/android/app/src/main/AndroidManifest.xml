<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for every network call this app makes (API, media upload/download,
         realtime WebSocket) — Flutter's tooling auto-adds this for debug/profile
         builds only (see src/debug and src/profile's own manifests, which exist
         purely for hot-reload's own connection), so a release build silently has NO
         network access at all without it declared here too. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Voice calling (features/calls/) — flutter_webrtc's getUserMedia needs mic
         access; MODIFY_AUDIO_SETTINGS backs Helper.setSpeakerphoneOn's earpiece/
         speaker toggle. Requested at runtime too (permission_handler, in
         CallController._ensureMicPermission) — the manifest entry alone only
         declares intent, Android still prompts the user the first time. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <!-- Biometric unlock (features/auth/biometric_unlock.dart) — local_auth's
         BiometricPrompt integration needs this declared or the prompt silently
         refuses to show on API 28+. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Local message notifications (features/notifications/) — required at
         runtime on Android 13+ (API 33), requested explicitly in
         local_notifications.dart's initLocalNotifications; this declaration alone
         only states intent, same as RECORD_AUDIO above. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Incoming-call notification (local_notifications.dart's showIncomingCallNotification,
         fullScreenIntent: true) — lets that notification actually turn the screen on and
         launch MainActivity over the lock screen, matching WhatsApp's own incoming-call
         behavior, instead of just sitting silently in the tray until the phone is unlocked.
         Android 14+ (API 34) additionally gates this at runtime — requested explicitly via
         requestFullScreenIntentPermission() in initLocalNotifications. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <application
        android:label="Comm"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Required for flutter_local_notifications' notification ACTION BUTTONS
             (Accept/Decline on the incoming-call notification) to work at all — without
             this declared, the plugin's PendingIntent.getBroadcast(..., ActionBroadcastReceiver)
             targets an undeclared component and Android silently drops the broadcast: the
             button visibly does nothing, no Dart callback ever runs, the notification doesn't
             even dismiss. This is exactly the reported "nothing happens when clicking Decline"
             bug — found by reading flutter_local_notifications' own Android source
             (ActionBroadcastReceiver.java + its example app's manifest), not present anywhere
             in this file until now because nothing before the Accept/Decline buttons ever used
             a notification action. -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
