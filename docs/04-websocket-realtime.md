# 04 — WebSocket / Realtime Architecture

**Revision note**: implemented via `apps/web/server.ts`, a custom Next.js server — a plain Node `http.Server` that hands normal HTTP requests to Next's request handler and, on the server's `upgrade` event for the `/realtime` path, hands the connection to a `ws.Server` instead (no separate NestJS WebSocket gateway/Fastify process). See [00-overview](00-overview.md) and [01-folder-structure](01-folder-structure.md).

## Transport & auth

Single authenticated WebSocket connection per device (`wss://.../realtime`), upgraded only after the access token is validated during the HTTP upgrade handshake (short-lived token, same one used for REST — no separate weaker WS auth path; the same `requireAuth()` helper the Route Handlers use is called here against the upgrade request's cookies before `ws.Server` completes the handshake). The socket is bound to `(userId, deviceId, sessionId)` server-side at connect time; every inbound event is authorized against that binding, never against a client-claimed field in the message body. A revoked session/device is force-disconnected immediately (the revoke handler publishes to Redis, every `apps/web` process holding that socket closes it — see [06-device-architecture](06-device-architecture.md)).

Multiple tabs/processes: each browser tab opens its own socket (its own `deviceId`-scoped identity isn't shared — see multi-device model in [06](06-device-architecture.md)); fan-out to "all of a user's devices" is done server-side via a Redis-backed registry of `userId → [socketId...]` per process, not by the client.

## Scaling across processes

Redis pub/sub is the cross-instance bus: when one `apps/web` process needs to deliver an event to a device connected to a different process (multiple PM2/systemd instances behind the reverse proxy, see [11-deployment-architecture](11-deployment-architecture.md)), it publishes to a per-device Redis channel; the owning process (subscribed) forwards to its local socket. Presence and typing state live in Redis with short TTLs (ephemeral by design — see [10-privacy-data-retention](10-privacy-data-retention.md)), not in Postgres.

`apps/worker` also subscribes to `MESSAGE_EVENTS_CHANNEL` (not just `apps/web`) — its push-notification dispatch (docs/13-roadmap.md's push notification pass, `apps/worker/src/realtime/push-dispatch.ts`) reacts to the same `new` events a connected device's socket would, deciding whether to send a Web Push for devices that either aren't currently connected or just haven't opened the relevant tab. This is why `MESSAGE_EVENTS_CHANNEL`'s channel name and `MessageEvent` union live in `@comm/types` rather than `apps/web` — a second, independent app subscribing to the exact same channel is the whole reason that boundary exists (see the `deleted` event row below for the first case this pattern was built for).

## Event catalog

**Shipped (Phase 3)** — inbound events are validated Zod envelopes (`apps/web/server/realtime/message-handlers.ts`); outbound events are the `MessageEvent` union published over Redis (`apps/web/server/realtime/bus.ts`) and forwarded verbatim to the target device's socket(s):

| Direction | Event `type` | Payload shape | Notes |
|---|---|---|---|
| C→S | `message.send` | `{ type, conversationId, recipientDeviceId, envelopeType, envelopeHeader?, ciphertext, x3dhInit?, contentTypeHint, messageId, sentAt, replyToMessageId? }` | `messageId` is client-generated (idempotency key, see [02-database-schema](02-database-schema.md#message-ids)); server responds `{ type: 'message.sent', messageId, serverReceivedAt }` |
| S→C | `new` | `{ type: 'new', targetDeviceId, message: MessageDto }` | Fanned out to every other member's every active device *and* the sender's own other devices (docs/13-roadmap.md's multi-device sync pass — one `MessageDto` per target device, each carrying its own Double-Ratchet envelope for `direct` conversations; Phase 3 originally addressed only a single guessed recipient device, see that pass's own writeup for why that needed a schema change, not just a bigger loop) |
| C→S | `message.ack` | `{ type, messageId }` | Server writes `message_recipients.delivered_at`; no response event (fire-and-forget) |
| S→C | `delivered` | `{ type: 'delivered', targetDeviceId, messageId, deliveredAt }` | Pushed to the sender's device |
| C→S | `message.read` | `{ type, conversationId, upToMessageId }` | Advances `conversation_members.last_read_message_id`; server checks the reader's `read_receipts` privacy setting before fanning out (not the sender's) |
| S→C | `read` | `{ type: 'read', targetDeviceId, conversationId, upToMessageId, readAt }` | Pushed to the other member's device |
| C→S | `typing.start` / `typing.stop` | `{ type, conversationId }` | Membership-checked, and gated on the sender's own `typing_indicators` privacy setting (an opted-out sender's client never even reaches the fan-out step) |
| S→C | `typing` | `{ type: 'typing', targetDeviceId, conversationId, fromUserId, state: 'start'\|'stop' }` | Not persisted anywhere — Redis pub/sub only, no TTL'd state table yet (state is transient by construction: only forwarded while both sockets are live) |
| S→C | `error` | `{ type: 'error', code: AppErrorCode, message }` | Response to a malformed or unauthorized inbound event, same `AppErrorCode`s as REST ([03-api-design](03-api-design.md#error-codes)) |
| S→C | `deleted` | `{ type: 'deleted', targetDeviceId, conversationId, messageId, reason }` | Scrubs a tombstoned message from the recipient's locally-decrypted cache (lib/crypto/message-cache.ts), not just the server row. `reason` (`'manual' \| 'disappearing_timer' \| 'media_retention'`, docs/02-database-schema.md's `deletion_reason`) is what lets the client show "deleted" vs. "disappeared" vs. "expired" instead of one generic message. Published from three places: `DELETE /api/messages/:id` (a manual delete) and `apps/worker`'s two independent sweeps — disappearing-message expiry and the 24h media retention default (docs/10-privacy-data-retention.md) — both background jobs, not request handlers, so neither can call `apps/web/server/realtime/bus.ts`'s publish functions directly; all three instead publish the exact same shape onto `MESSAGE_EVENTS_CHANNEL`, whose channel name and event union live in `@comm/types` specifically so a different app can produce it without importing from `apps/web` |
| S→C (out-of-band) | WS close code `4001` | — | Sent (not a JSON event) when the socket's device is revoked — see [06-device-architecture](06-device-architecture.md); client distinguishes this from a generic disconnect via the close code |
| C→S | `call.invite` | `{ type, conversationId, callId, sdp }` | Rings every active device of the other conversation member (`getAllOtherMembersActiveDeviceIds` — the single-guessed-device heuristic this originally used, `getPrimaryRecipientDevice`, was replaced by the multi-device sync pass above); `sdp` is the caller's SDP offer, generated only after mic access is granted client-side. Rate-limited more strictly than other call events (6/min) — ringing is disruptive in a way sending a message isn't |
| S→C | `call.ring` | `{ type: 'call.ring', targetDeviceId, conversationId, callId, fromUserId, fromDisplayName, sdp }` | `fromDisplayName` is resolved server-side (not trusted from the client, and not assumed to already be in the callee's local cache) |
| C→S | `call.answer` | `{ type, conversationId, callId, sdp }` | `sdp` is the callee's SDP answer |
| S→C | `call.answered` | `{ type: 'call.answered', targetDeviceId, conversationId, callId, sdp }` | |
| C→S / S→C | `call.ice-candidate` | `{ type, conversationId, callId, candidate }` (C→S) / adds `targetDeviceId` (S→C) | Relayed as fast as gathered on either side; buffered client-side until the corresponding remote SDP has been applied (ICE can arrive before `call.answered`/before the callee has accepted at all) |
| C→S | `call.reject` | `{ type, conversationId, callId, reason: 'declined' \| 'busy' \| 'no-answer' }` | Sent by the callee (decline), or by either side auto-rejecting as `busy` if already on a call |
| S→C | `call.rejected` | `{ type: 'call.rejected', targetDeviceId, conversationId, callId, reason }` | |
| C→S | `call.end` | `{ type, conversationId, callId }` | Hangup (mid-call) or cancel (caller gives up while still ringing — also sent client-side by the 45s no-answer timeout) |
| S→C | `call.ended` | `{ type: 'call.ended', targetDeviceId, conversationId, callId }` | |
| C→S | `group.key-share` | `{ type, groupId, epoch, toDeviceId, envelope, x3dhInit? }` | Group chat pass (docs/13-roadmap.md), shipped. A blind device-to-device relay exactly like `call.*` signaling, except the payload is also durably persisted (`GroupKeyShare` row, [02-database-schema](02-database-schema.md#group_key_shares)) before being forwarded — a member offline at share time still catches up via the REST route below. Not currently exercised by the shipped client (which posts key-shares via `POST /api/groups/:id/key-shares` exclusively, after a real bug: a fire-and-forget WS send silently drops if the socket isn't open yet, which reliably happened immediately after creating a group). Kept as a redundant, already-authorized fast path rather than removed |
| S→C | `group.key-share` | `{ type, targetDeviceId, groupId, keyShareId }` | A live nudge that a `GroupKeyShare` row is waiting — the client fetches it via `GET /groups/:id/key-shares` rather than the payload riding the socket event itself. This is the "don't wait for your next poll" fast path; the REST route is what makes delivery durable for an offline member |
| S→C | `group.members-changed` | `{ type, targetDeviceId, groupId, conversationId }` | Published on add/remove. Tells already-connected member clients to react — a removed member is excluded going forward (fresh epoch, redistributed only to current members); a newly-added member's client fetches the group's *current* outbound sessions, no retroactive history access |
| C→S | `group-call.start` / `group-call.join` / `group-call.leave` | `{ type, conversationId, callId }` | Group calls (docs/13-roadmap.md), shipped — a separate namespace/channel from `call.*` above, see the "Group call signaling" section below. `start`/`join` rate-limited via `groupCallStart` (6/min, as tight as `call.invite`); `leave` shares `groupCallSignal` with the peer-signal events below |
| S→C | `group-call.invited` | `{ type, targetDeviceId, conversationId, callId, fromUserId, fromDisplayName, groupName }` | Fanned to every other member's every active device via `getAllOtherMembersActiveDeviceIds`, same as `call.ring`; unlike `call.ring`, there's no per-device reply expected (no `group-call.ringing`) |
| S→C | `group-call.roster` | `{ type, targetDeviceId, conversationId, callId, participants: GroupCallParticipant[] }` | Sent only to a joining device right after `group-call.join` — informational (seeds its participant tiles); the joiner does NOT open connections from this, see the row below |
| S→C | `group-call.participant-joined` / `group-call.participant-left` | `{ type, targetDeviceId, conversationId, callId, participant }` / `{ ..., userId, deviceId }` | Fanned to every already-there participant. By convention, existing participants always initiate the offer *to* a new joiner (avoids SDP glare — a joiner only ever answers) |
| S→C | `group-call.ended` | `{ type, targetDeviceId, conversationId, callId }` | Fanned to every OTHER group member's every active device (not just roster participants) once the roster empties — a member who never joined still got `group-call.invited` and needs the stale "ongoing call" indicator cleared too |
| C→S / S→C | `group-call.offer` / `group-call.answer` / `group-call.ice-candidate` | `{ type, conversationId, callId, targetUserId, targetDeviceId, sdp \| candidate }` (C→S) / adds `fromUserId, fromDeviceId` (S→C) | Pairwise, unlike every `call.*` signaling event above — explicitly targeted at one device, relayed only after verifying that device genuinely belongs to a real member of this conversation (`assertValidGroupCallTarget`), never blindly. Rate-limited via `groupCallSignal` (400/min — deliberately more generous than `call.signal`'s 120/min, since one join near a full room opens up to `GROUP_CALL_MAX_PARTICIPANTS - 1` simultaneous exchanges instead of 1:1's single pair) |

**Not yet shipped**: `presence.update` (online/offline/last-seen — needs the presence registry this doc's "Scaling across processes" section describes; not built), `key.changed` (identity-key-change notification), `sync.required` (see the honest note in Reconnection below — the client currently just re-fetches via REST on every reconnect rather than the server flagging that a fetch is needed).

## Reconnection & delivery guarantees

- **At-least-once, idempotent.** Every message carries a client-generated UUIDv7 id; the server's insert is `ON CONFLICT (id) DO NOTHING`, so a client that retries a send after a dropped ack never creates a duplicate. Clients dedupe incoming `new` events by id for the same reason (a redelivery after reconnect is a no-op, not a duplicate bubble).
- **Reconnect backoff**: exponential with jitter, capped 1s → 30s (`apps/web/lib/realtime-client.ts`), resets to the fast end on a successful connect. Respects close code `4001` (device revoked) by not reconnecting at all.
- **Missed-event catch-up — shipped, simpler than originally designed.** There is no `sync.required` server push and no cursor negotiated over the socket. Instead, the open conversation thread simply re-fetches via `GET /conversations/:id/messages` whenever the connection (re)opens, which is sufficient at Phase 3's scale (a page/tab with one open conversation) but not yet a general per-conversation-list catch-up — that's a real gap to close before Phase 3's guarantees extend to "every conversation in the sidebar," not a documentation lag.
- **Multiple tabs**: each tab is its own device identity, so "duplicate delivery across tabs" isn't a special case — it's just two devices receiving the same message, each with its own delivery/read tracking. Read state at the *conversation* level (for unread-count purposes) is reconciled via `conversation_members.last_read_message_id`, which any of the user's devices can advance.
- **Server restart**: sockets drop, clients reconnect via the backoff above and catch up via the refetch-on-reconnect behavior above — no in-memory state the server holds is authoritative (Postgres and Redis are), so a restart never loses data, only causes a brief reconnect.

## Call signaling — shipped (1:1 audio, ahead of the rest of Phase 6)
<a id="call-signaling"></a>
Call setup/teardown and SDP/ICE exchange ride the same authenticated socket under a `call.*` namespace, per the event catalog above — one authenticated channel to secure and monitor, not two (`server/realtime/message-handlers.ts`). The server relays signaling blindly between the two conversation members (authorized by re-deriving membership on every event; call *history* persistence — `calls` rows — shipped later, docs/13-roadmap.md); it does not, and structurally cannot, inspect SDP contents to affect the call. Media itself never touches this channel or the API server — see [11-deployment-architecture](11-deployment-architecture.md) for the TURN path, and [08-threat-model](08-threat-model.md#call-security) for the full security reasoning. Shipped as **audio-only, direct conversations only, no minimize-to-bubble** — a deliberately narrow slice, with video and call-history-UI-on-web explicitly still open (docs/13-roadmap.md). Group calls shipped later under their own namespace — see immediately below.

## Group call signaling — shipped (mesh audio, docs/13-roadmap.md)
<a id="group-call-signaling"></a>
A deliberately separate namespace/channel from `call.*` above (`group-call.*`, own `GROUP_CALL_EVENTS_CHANNEL`), not a reuse: 1:1's payloads have no per-peer target field, correct for its own single-pair broadcast fan-out but wrong once 3+ people are negotiating — member B's answer would otherwise get broadcast to member C too, corrupting C's own pending negotiation. Every `group-call.offer`/`answer`/`ice-candidate` payload instead carries an explicit `targetUserId`/`targetDeviceId`, verified server-side against real conversation membership before a blind relay (`assertValidGroupCallTarget`, `server/modules/calls/group-service.ts`) — never trusting a client-claimed target the way `call.*` can lean on "the other conversation member" alone.

Mesh, not an SFU: every participant holds a direct `RTCPeerConnection` to every other participant, capped at `GROUP_CALL_MAX_PARTICIPANTS = 6` (packages/types/src/calls.ts). The live roster during an active call is Redis-only (`group-call-roster:<callId>`, TTL'd as an orphan safety net, not the primary cleanup path); `Call`/`calls` rows are still reused as-is for history, one row per call regardless of participant count. No per-participant "ringing" state the way `call.*` has — declining a `group-call.invited` banner is simply not sending `group-call.join`, with no signal sent back to the room (see the event catalog's own note on this asymmetry). Audio-only, same as 1:1; no video plumbing exists anywhere in this app yet.

## What the server does *not* do here

It doesn't decrypt `ciphertext` to decide anything (routing is by `conversationId`/membership, not content). It doesn't infer `content_type_hint` from the payload — the sender asserts it, and a lying client can only misrender its own message for others (a client-side annoyance, not a security boundary). It doesn't hold typing/presence state longer than a short TTL — see [10-privacy-data-retention](10-privacy-data-retention.md).
