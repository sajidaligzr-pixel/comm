{
  "name": "@comm/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Zod schemas + inferred TypeScript types — the single source of truth for API/WS contracts (docs/03-api-design.md).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint ."
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@comm/config": "*",
    "typescript": "^5.6.3"
  }
}
