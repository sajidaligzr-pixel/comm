{
  "name": "@comm/security",
  "version": "0.0.0",
  "private": true,
  "description": "Argon2id password hashing, secure random tokens, and Redis-backed rate limiting — reviewed in isolation (docs/79 crypto/security package rule). Not the E2E message crypto package; see packages/crypto (Phase 3) for that.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "argon2": "^0.41.1",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@comm/config": "*",
    "typescript": "^5.6.3",
    "vitest": "^3.2.7"
  }
}
